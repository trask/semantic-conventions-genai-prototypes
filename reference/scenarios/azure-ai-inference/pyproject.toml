[project]
name = "azure-ai-inference-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "azure-ai-inference==1.0.0b9",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
