[project]
name = "aws-bedrock-agent-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "boto3==1.42.96",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
