[project]
name = "cohere-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "cohere==6.1.0",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
