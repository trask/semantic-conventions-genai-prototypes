[project]
name = "haystack-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "haystack-ai==2.28.0",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
